import 'package:flutter/material.dart';

class TradeHomeScreen extends StatelessWidget {
  const TradeHomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Trade Home Page'));
  }
}
